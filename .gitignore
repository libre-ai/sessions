/target
**/*.rs.bk
.env
/dev/keycloak/.env
.DS_Store

# Generated by scripts/build-owner-app.sh / build-join-app.sh. CI packages and
# verifies these exact bundles before every server build; they are deliberately
# not source-controlled.
/crates/server/static/owner-app/
/crates/server/static/join-app/

# Coverage artifacts
cobertura.xml
*.profraw
*.profdata
